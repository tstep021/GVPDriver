This application reads the call flow configuration for each incoming telephone call from the database,
builds the call flow at runtime, and then invokes application nodes accordingly, for the 
IVR system to interact with caller

!!ATTENTION!!
Driver uses context /GLOBAL_RESOURCES 
 as a resource repository - configurations, grammars, prompts and recordings